[package]
name = "afterrayd"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
afterray-agent = { path = "../afterray-agent" }
afterray-codec = { path = "../afterray-codec" }
afterray-harness = { path = "../afterray-harness" }
afterray-models = { path = "../afterray-models" }
afterray-platform-macos = { path = "../afterray-platform-macos" }
afterray-protocol = { path = "../afterray-protocol" }
afterray-store = { path = "../afterray-store" }
anyhow.workspace = true
blake3.workspace = true
chrono.workspace = true
dirs.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
uuid.workspace = true

[dev-dependencies]
# Only for the live-Ollama probe in `stream.rs`, which asks /api/tags
# which model this machine actually has before running against it.
reqwest.workspace = true
tempfile = "3.27"

[lints]
workspace = true
