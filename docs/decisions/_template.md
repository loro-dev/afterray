# Decision: <one-line title, stating the choice and not the topic>

Status: proposed
Area: <capture | store | models | recall-ui | agent | release | privacy>
Anchors:
- <path> @dec:<slug>
Supersedes: —
Superseded-by: —

## Problem

What forced a choice. Write it so it stands without the solution — someone who disagrees with the decision should still recognise the problem.

## Proposal

<!-- `active/` and `superseded/` use `## Decision` here instead, in the present tense. -->

What changes. May speak in the future tense while the work is unbuilt.

## Alternatives considered

**<The alternative>.** What it would have done and why it lost. One paragraph each, for every option genuinely on the table.

<!-- Writing this up after the fact and the alternatives are not reconstructible?
     Say `Not recorded; reconstructed from <source>` and list only what the source supports.
     Do not invent alternatives. -->

## Acceptance criteria

<!-- `proposed/` only. What observable state means this is done. -->

## Risks

<!-- `proposed/` only. What could go wrong, and what the decision knowingly gives up. -->

## Consequences

<!-- `active/` and `superseded/` only. What the trade-off cost *and* what it bought.
     Link any postmortem this decision contributed to. -->
